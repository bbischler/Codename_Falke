import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { StatsmodalPage } from './statsmodal';

@NgModule({
  declarations: [
    StatsmodalPage,
  ],
  imports: [
    IonicPageModule.forChild(StatsmodalPage),
  ],
})
export class StatsmodalPageModule {}
